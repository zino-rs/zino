[package]
name = "minimal-app"
description = "A minimal example to run a server."
version = "0.9.0"
rust-version = "1.80"
edition = "2021"
publish = false

[dependencies.zino]
path = "../../crates/zino"
version = "0.32.0"
features = ["axum"]

