[workspace]
resolver = "2"
members = [
    "zino",
    "zino-cli",
    "zino-core",
    "zino-derive",
    "zino-dioxus",
    "zino-extra",
    "zino-model",
]

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
